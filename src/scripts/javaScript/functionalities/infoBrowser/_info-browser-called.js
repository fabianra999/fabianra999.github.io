/**
 *  Llamados
 *
 ==============================================================================*/

/**
 * Init
 */
(function () {
  setTimeout(function () {
    //detectBrowser();
    //mobileDetecting();
    //viewportSize();
  }, 3000);

  if ($('body').is('.mobile')) {

  }
})();

/**
 * Scroll
  */
window.onscroll = function () {
  //detectBrowser();
  //mobileDetecting();
  //viewportSize();
}

/**
 * Resize
 */
window.addEventListener("resize", function () {
  //detectBrowser();
  //mobileDetecting();
  //viewportSize();
});

/**
 * load
 */
window.onload = function() {
  // my function loader
};
