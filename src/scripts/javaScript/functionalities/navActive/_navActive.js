/**
 * Nav Active
 */
function navActive() {
    let pats = window.location.pathname.split( '/' );
    for(let value of mira) {
        console.log(value);
    }
}

(function() {
    navActive();
})()